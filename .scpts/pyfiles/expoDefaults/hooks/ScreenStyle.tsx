/**
 * handles uniform styling for all screens
 */

import { StyleSheet } from 'react-native';

const ScreenStyle = StyleSheet.create({
	allScreenContainer: {
        flex: 1,
        paddingHorizontal: 10,
    },
})
export default ScreenStyle;